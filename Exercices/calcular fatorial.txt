let num = prompt("Insira um número para ser fatorado: ")

function calcFatorial(num) {
    fatorar = 1
    for (let i = 1; i <num; i++) {
        resultado *= i;
    }
    return fatorar;
}

console.log(calcFatorial(num))
